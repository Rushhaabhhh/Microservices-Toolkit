{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016",                                /* Specify ECMAScript target version. */
    "module": "commonjs",                              /* Specify module system. */
    "outDir": "./dist",                                /* Directory for output files. */
    "esModuleInterop": true,                           /* Allow default imports for CommonJS modules. */
    "forceConsistentCasingInFileNames": true,          /* Enforce consistent case-sensitive file imports. */
    "strict": true,                                    /* Enable all strict type-checking options. */
    "skipLibCheck": true,                              /* Skip type checking for .d.ts files. */

    /* Interop Constraints */
    "allowSyntheticDefaultImports": true,             /* Allow 'import x from y' when a module doesn't have a default export. */

    /* TypeScript Behavior */
    "resolveJsonModule": true                          /* Enable importing .json files. */
  },
  "include": ["src/**/*"],
  "exclude": [
    "node_modules", 
    "dist"
  ]
}
